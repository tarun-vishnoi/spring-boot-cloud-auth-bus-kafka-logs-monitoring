spring:
  application:
    name: zuul
  cloud:    config:      uri:      - http://localhost:8763/
    
server:
  port: 8762

logging:
  file: E:/logs/zuul.log
  level:
    root: INFO
    org.springframework.web: INFO