spring:
  application:
    name: consumer
  cloud:
    config:
      uri:
      - http://localhost:8763/
    
server:
  port: 0
  

logging:
  file: E:/logs/consumer.log
  level:
    root: INFO
    org.springframework.web: INFO
    org.hibernate : INFO