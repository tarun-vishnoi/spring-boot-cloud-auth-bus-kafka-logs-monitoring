spring:
  application:
    name: producer
  cloud:
    config:
      uri:
      - http://localhost:8763/
      
server:
  port: 0
 
      
logging:
  file: E:/logs/producer.log
  level:
    root: INFO
    org.springframework.web: INFO